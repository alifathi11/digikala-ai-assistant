comparison_evaluation:
  weights:
    correctness: 0.20
    groundedness: 0.20
    criterion_coverage: 0.15
    conflict_handling: 0.10
    recommendation_calibration: 0.15
    relevance: 0.10
    instruction_following: 0.05
    safety: 0.05

  judge:
    # null => same model as configs/qa.yaml
    model: null
    input_cost_per_million: null
    output_cost_per_million: null
    max_context_chars: 18000
    max_chars_per_review: 900

  # Benchmark is fixed before evaluation. No model/weight tuning is done here.
  report_split: test
