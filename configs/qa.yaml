generation:
  provider: openai_compatible
  model: gpt-5.6-terra

  # Fill these only if you know the API provider's exact pricing.
  # Keeping them null still records token usage.
  input_cost_per_million: null
  output_cost_per_million: null

qa:
  top_k: 8
  max_context_chars: 10000
  max_chars_per_comment: 1500